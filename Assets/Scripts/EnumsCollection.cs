public enum EPlayerOrientation {
    None,
    Top,
    Bottom,
    Left,
    Right,
}

public enum ERotationDirection {
    Up,
    Down,
}

public enum ECurrentView {
    MainMenu,
    PauseMenu,
    Game,
    WinScreen,
    LoseScreen,
    LevelSelect,
    GameOverScreen,
}

public enum EActionMapping {
    UI,
    Game,
}

public enum InputType {
    Keyboard,
    Controller
}